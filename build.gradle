// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        jcenter()

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://github.com/im97mori-github/maven/raw/master" }
//        maven { url System.getenv('MAVEN_REPOSITORY_URL') }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

task archiveAll {
}

archiveAll {
    dependsOn ":core:uploadArchives", ":advertising:uploadArchives", ":characteristic:characteristic-dis:uploadArchives", ":characteristic:characteristic-gas:uploadArchives", ":descriptor:uploadArchives", ":library-central:uploadArchives", ":library-peripheral:uploadArchives"
}

ext.stacklogVersionName = "0.0.2"

ext.coreVersionName = "0.1.5"
ext.coreVersionCode = 6

ext.advertisingVersionName = "0.1.5"
ext.advertisingVersionCode = 6

ext.disVersionName = "0.1.5"
ext.disVersionCode = 6
ext.gapsVersionName = "0.1.1"
ext.gapsVersionCode = 2
ext.gasVersionName = "0.1.5"
ext.gasVersionCode = 6
ext.basVersionName = "0.1.0"
ext.basVersionCode = 1

ext.descriptorVersionName = "0.1.5"
ext.descriptorVersionCode = 6

ext.centralVersionName = "0.4.6"
ext.centralVersionCode = 16

ext.peripheralVersionName = "0.1.11"
ext.peripheralVersionCode = 11