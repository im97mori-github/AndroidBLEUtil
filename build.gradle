// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        jcenter()

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://github.com/im97mori-github/maven/raw/master" }
//        maven { url System.getenv('MAVEN_REPOSITORY_URL') }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

task archiveAll {
}

archiveAll {
    dependsOn ":core:uploadArchives", ":advertising:uploadArchives", ":characteristic:characteristic-dis:uploadArchives", ":characteristic:characteristic-gas:uploadArchives", ":descriptor:uploadArchives", ":library-central:uploadArchives", ":library-peripheral:uploadArchives"
}

ext.stacklogVersionName = "0.0.2"

ext.coreVersionName = "0.1.6"
ext.coreVersionCode = 7

ext.advertisingVersionName = "0.1.6"
ext.advertisingVersionCode = 7

ext.disVersionName = "0.1.6"
ext.disVersionCode = 7
ext.gapsVersionName = "0.1.2"
ext.gapsVersionCode = 3
ext.gasVersionName = "0.1.6"
ext.gasVersionCode = 7
ext.basVersionName = "0.1.1"
ext.basVersionCode = 2
ext.ctsVersionName = "0.1.0"
ext.ctsVersionCode = 1

ext.descriptorVersionName = "0.1.6"
ext.descriptorVersionCode = 7

ext.centralVersionName = "0.4.7"
ext.centralVersionCode = 17

ext.peripheralVersionName = "0.1.12"
ext.peripheralVersionCode = 12