apply plugin: 'com.android.library'
apply plugin: 'digital.wup.android-maven-publish'

def target = serviceDisAndroid
def GROUP_ID = groupId
def ARTIFACT_ID = target[artifactId]
def VERSION_CODE = target[versionCode]
def VERSION_NAME = target[uploadVersionName]

android {
    compileSdkVersion globalCompileSdkVersion
    buildToolsVersion globalBuildToolsVersion

    defaultConfig {
        minSdkVersion globalMinSdkVersion
        targetSdkVersion globalTargetSdkVersion
        versionCode VERSION_CODE
        versionName VERSION_NAME

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    api "${groupId}:${characteristicU2a23Android.artifactId}:${characteristicU2a23Android.versionName}"
    api "${groupId}:${characteristicU2a24Android.artifactId}:${characteristicU2a24Android.versionName}"
    api "${groupId}:${characteristicU2a25Android.artifactId}:${characteristicU2a25Android.versionName}"
    api "${groupId}:${characteristicU2a26Android.artifactId}:${characteristicU2a26Android.versionName}"
    api "${groupId}:${characteristicU2a27Android.artifactId}:${characteristicU2a27Android.versionName}"
    api "${groupId}:${characteristicU2a28Android.artifactId}:${characteristicU2a28Android.versionName}"
    api "${groupId}:${characteristicU2a29Android.artifactId}:${characteristicU2a29Android.versionName}"
    api "${groupId}:${characteristicU2a2aAndroid.artifactId}:${characteristicU2a2aAndroid.versionName}"
    api "${groupId}:${characteristicU2a50Android.artifactId}:${characteristicU2a50Android.versionName}"
}

task sourcesJar(type: Jar) {
    archiveClassifier.set('sources')
    from android.sourceSets.main.java.srcDirs
}

publishing {
    publications {
        mavenAar(MavenPublication) {
            groupId GROUP_ID
            artifactId ARTIFACT_ID
            version VERSION_NAME
            artifact(sourcesJar)
            from components.android
            pom {
                name = 'BLE Core Utility for Android'
                url = 'https://github.com/im97mori-github/AndroidBLEUtil'
                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                        distribution = 'repo'
                    }
                }
                developers {
                    developer {
                        email = 'github@im97mori.org'
                    }
                }
            }
        }
    }
    repositories {
        maven {
            url System.getenv('MAVEN_REPOSITORY_URL')
        }
    }
}